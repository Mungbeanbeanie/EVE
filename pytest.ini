[pytest]
asyncio_mode = auto
testpaths = tests
# test_audio.py is a manual hardware smoke script (opens a real mic at import),
# not a unit test — skip it during automated collection.
addopts = --ignore=tests/test_audio.py
markers =
    slow: tests that load/download models or shell out to system tools
